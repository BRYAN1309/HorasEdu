import type React from 'react';
import type {FinalExam} from '../types/types';

export const viewFinalExam = async (id: number, setter: React.Dispatch<React.SetStateAction<FinalExam | undefined>>) => {
	try {
	} catch (err) {
		throw err;
	}
};

export const submitFinalExam = async (score: number, id: number) => {
	try {
	} catch (err) {
		throw err;
	}
};
